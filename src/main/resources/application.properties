spring.application.name=biblioteca-api

#spring.datasource.url=jdbc:postgresql://localhost:5432/api-rpg

#spring.datasource.username=postgres
#spring.datasource.password=root

#spring.datasource.driver-class-name=org.postgresql.Driver

#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

#spring.jpa.hibernate.ddl-auto=update

#spring.jpa.show-sql=true

# Nível raiz (padrão é INFO)
logging.level.root=INFO

# Nível específico para o seu pacote
logging.level.com.seuexemplo=DEBUG

# Salvar logs em um arquivo
logging.file.name=logs/application.log
# Ou logging.file.path=logs/

# Configuração de rotação (arquivos antigos)
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=7


server.port=8069